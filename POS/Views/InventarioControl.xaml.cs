using System.Windows.Controls;

namespace POS.Views
{
    /// <summary>
    /// Control de usuario que encapsula la interfaz de Gestión de Inventario.
    /// Se utiliza dentro del MainView para la navegación por módulos.
    /// </summary>
    public partial class InventarioControl : UserControl
    {
        public InventarioControl()
        {
            InitializeComponent();
        }
    }
}
