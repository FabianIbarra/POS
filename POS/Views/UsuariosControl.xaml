<UserControl
    x:Class="POS.Views.UsuariosControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource AppBackgroundBrush}">

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Formulario  -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,15"
                    Style="{StaticResource SectionHeaderStyle}"
                    Text="Datos del Usuario" />

                <TextBlock
                    Margin="0,0,0,6"
                    Style="{StaticResource LabelTextStyle}"
                    Text="Nombre de Usuario:" />
                <TextBox
                    PreviewTextInput="UsernameTextBox_PreviewTextInput"
                    DataObject.Pasting="UsernameTextBox_Pasting"
                    TextChanged="UsernameTextBox_TextChanged"
                    Style="{StaticResource TextBoxStyle}"
                    Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock
                    Margin="0,12,0,6"
                    Style="{StaticResource LabelTextStyle}"
                    Text="Nombre Completo:" />
                <TextBox Style="{StaticResource TextBoxStyle}" Text="{Binding NombreCompleto, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock
                    Margin="0,12,0,6"
                    Style="{StaticResource LabelTextStyle}"
                    Text="Contraseña:" />
                <PasswordBox
                    x:Name="PasswordBoxContrasena"
                    PasswordChanged="PasswordBoxContrasena_PasswordChanged"
                    Style="{StaticResource PasswordBoxStyle}" />

                <TextBlock
                    Margin="0,12,0,6"
                    Style="{StaticResource LabelTextStyle}"
                    Text="Rol:" />
                <ComboBox
                    ItemsSource="{Binding Roles}"
                    SelectedItem="{Binding RolSeleccionado}"
                    Style="{StaticResource ComboBoxStyle}" />

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button
                        Grid.Column="0"
                        Command="{Binding GuardarUsuarioCommand}"
                        Content="Guardar"
                        Style="{StaticResource SuccessButtonStyle}" />
                    <Button
                        Grid.Column="2"
                        Command="{Binding LimpiarFormularioCommand}"
                        Content="Limpiar"
                        Style="{StaticResource NeutralButtonStyle}" />
                </Grid>
            </StackPanel>
        </Border>

        <!--  Tabla  -->
        <DataGrid
            Grid.Column="2"
            ItemsSource="{Binding Usuarios}"
            RowHeight="36"
            Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Username}"
                    Header="Usuario" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding NombreCompleto}"
                    Header="Nombre Completo" />
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Rol}"
                    Header="Rol" />
                <DataGridTemplateColumn Width="140" Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Margin="0,0,5,0"
                                    Command="{Binding DataContext.SeleccionarUsuarioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="Editar"
                                    Style="{StaticResource EditButtonStyle}" />
                                <Button
                                    Command="{Binding DataContext.EliminarUsuarioCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="Eliminar"
                                    Style="{StaticResource DeleteButtonStyle}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
